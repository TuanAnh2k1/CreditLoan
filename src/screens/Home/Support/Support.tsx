import React from 'react';
import {StyleSheet, View} from 'react-native';

const Support = () => {
  return <View style={styles.container} />;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#025e5c',
  },
});

export default Support;
